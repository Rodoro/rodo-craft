class NAV_BAR {
    private root = '/'

    MAIN = `${this.root}`
    SERVERS = `${this.root}servers`
    VACANCY = `${this.root}vacancy`
    WIKI = `${this.root}wiki`
}

export const NAV_BAR_PAGE = new NAV_BAR()